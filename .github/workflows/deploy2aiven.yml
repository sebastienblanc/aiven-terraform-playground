steps:
- uses: hashicorp/setup-terraform@v2

- run: terraform init

- id: plan
  run: terraform plan -no-color
- id: apply
  run: terraform apply
  env:
    AIVEN_TOKEN: "${{ secrets.AIVEN_TOKEN }}"   